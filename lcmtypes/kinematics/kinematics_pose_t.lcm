package kinematics;

struct pose_t {
  int64_t utime; // microseconds since 1970-01-01T00:00:00
  double position[3]; // meters (north, east, down) in local coordinates
  double orientation[4]; // (h,i,j,k) quaternion from FSK body to NED local
}
